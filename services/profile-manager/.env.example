# Example environment variables for profile-manager
NODE_ENV=development
PORT=3000
MONGO_URI=mongodb://localhost:27017/profile-manager
# RSA keypair: preferred method is to supply a private key file via JWT_PRIVATE_KEY_PATH
# or the raw PEM contents via JWT_PRIVATE_KEY. The public key can be supplied via
# JWT_PUBLIC_KEY_PATH or JWT_PUBLIC_KEY.
JWT_SECRET=replace-this-with-a-secure-random-string
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
LOG_LEVEL=info


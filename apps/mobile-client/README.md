# Mobile Client (Expo)
React Native frontend using Expo.

## Setup
1. Copy `.env.example` to `.env` and configure your local network IP (e.g. `BASE_URL=http://YOUR_LOCAL_IP:3000`).
2. Run `npm install`
3. Run `npx expo start` to launch in development mode on your mobile device.

# Mobile Client (Expo)
React Native frontend using Expo.

## Setup
1. Update `app.json` and configure your full network API routing URL in `expo.extra.baseUrl` (e.g. `"baseUrl": "http://192.168.0.107:30000"`).
2. Run `npm install`
3. Run `npx expo start` to launch in development mode on your mobile device.
